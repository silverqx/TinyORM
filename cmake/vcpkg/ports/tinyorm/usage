The package @PORT@:@TARGET_TRIPLET@ is compatible with built-in CMake targets:

    find_package(TinyOrm ${minTinyOrmVersion} CONFIG REQUIRED)
    target_link_libraries(main PRIVATE TinyOrm::TinyOrm)
