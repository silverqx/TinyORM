# TinyORM Models

It must be in its own folder so that the entire folder with models.pri and .clang_tidy can be mounted (symlink, bind, or junction) to another project, e.g. TinyOrmPlayground. If it weren't needed, it would be in tests/database/models/.
